## 导数的定义

$f'(x_0)=\lim _{\triangle  x \rightarrow 0} \frac {f(x_0+\triangle  x)-f(x_0)}{\triangle x}=\lim _{  x \rightarrow x_0} \frac {f(x)-f(x_0)}{ x-x_0}$

**定理**： $可导 \Leftrightarrow 左右导数都存在且相等$

>导数就是一个特殊的极限，用来刻画函数在某一点的变化率。

### 可导必连续

若函数 $y = f(x)$ 在点 $x_0$ 处可导，则 $f(x)$ 在点 $x_0$ 处连续。

>指的是某一点连续，非整个函数。

### 可导和连续可导的关系和区别

#### 一阶可导

1. 可以求一阶导数

2. 求出的导数可能连续也可能不连续

3. **一阶导数不可以求极限**（不知道一阶导数是否连续）

#### 一阶连续可导

1. 可以求一阶导数

2. **导函数连续**，可以用 1 次 [[极限#4. 利用洛必达法则求极限|洛必达法则]]

3. **一阶导数可以求极限**

#### 二阶可导

1. 具有二阶导数

2. 但是二阶导数的连续性无法确定

3. 二阶导数不可以求极限

4. $f(x)$ 二阶可导，说明**一阶连续可导**，那么只能用1次洛必达法则

#### 二阶连续可导

1. 具有二阶导数

2. 它的二阶导数是连续的

3. 二阶导数可以求极限

4. $f(x)$ 二阶连续可导，可以用2次洛必达法则

## 微分的概念

若 $\triangle y= f(x_0 + \triangle x)-f(x_0)=A \triangle x + o(\triangle x)$ ，则称 $f(x)$ 在 $x_0$ 处**可微**。称 $A\triangle x$ 为**微分**，记为 $dy = A dx$ .

>微分是函数改变量的近似值，是函数改变量的一个**线性主部**。

## 导数与微分的几何意义

函数 $y=f(X)$ 在点 $x_0$ 处可微的充分必要条件是 $f(x)$ 在点 $x_0$ 处可导，且有 $dy=f'(x_0) \triangle x = f'(x_0) dx$ .

导数的几何意义： $f'(x_0)=tan \alpha$ **表示切线的斜率**。

微分的几何意义： $dy=f'(x_0) dx$ **切线上的增量**。

![](https://img.mileomni.com/1731243062009.png)

## 连续、可导、可微之间的关系

1. **连续不一定可导，连续不一定可微**，经典反例为 $f(x)=|x|$
2. **可导一定连续**
3. $可导\Leftrightarrow 可微$
4. $f(x)$ 可导 $f'(x)$ 连续
5. $f(x)$ 可导， $$\lim _{x \rightarrow x_0}f'(x)$$ 不一定存在，例如 $$ f(x)=\left\{ \begin{aligned} x^2 \sin \frac 1 x & , x \ne 0 \\ 0 , x=0\end{aligned} \right. $$ 处处可导，但是 $\lim _{x \rightarrow 0}f'(x)$ 不存在，从而 $f'(x)$ 在 $x=0$ 处也不连续。

![](https://img.mileomni.com/1731243062095.png)

## 求导公式

| 公式                                         | 公式                                                                          |
| ------------------------------------------ | --------------------------------------------------------------------------- |
| 1)  $(C)'=0$                               | 2) $(X_ \alpha)'=\alpha x^{\alpha -1}$                                      |
| 3) $(x^x)'=a^x ln a$                       | 4) $(e^x)'=e^x$                                                             |
| 5) $(log_a x)'=\frac 1 {x lna}$            | 6) $(ln\ |x\|)'=\frac 1 x$                                                   |
| 7) $(\sin x)'=\cos x$                      | 8) $(\cos x)'= - \sin x$                                                    |
| 9) $(\tan x)'=\sec ^2 x$                   | 10) $(\cot x)'=- \csc ^2 x$                                                 |
| 11) $(\sec x)'=\sec x \tan x$              | 12) $(\csc x)'=- \csc x \cot x$                                             |
| 13) $(\arcsin x)'=\frac 1 {\sqrt {1-x^2}}$ | 14) $(\arccos x)'=- \frac 1 { \sqrt {1-x^2}}$                               |
| 15) $(\arctan x)'=\frac 1 {1+x^2}$         | 16) $\newcommand{\arccot}{\mathrm{arccot}\,}(\arccot x)'=- \frac 1 {1+x^2}$ |

## 求导法则

### 复合函数求导法（链导法）

$\frac {dy}{dx}=\frac {dy}{du} \frac {du}{dx}=f'(u)\varphi  '(x)$

### 隐函数求导法

设 $y=y(x)$ 是由方程 $F(x,y)=0$ 所确定的**可导函数**，为求得 $y'$ ，可在方程 $F(x,y)=0$ 两边对 $x$ 求导，可得到一个含有 $y'$ 的方程，从中解出 $y'$ 即可。

>也可用**隐函数求导公式** $\frac {dy}{dx}=-\frac {F'_x}{F'_y}$ 得到。

>求完后能用原方程化简的要化简

### 反函数的导数

若 $x=\varphi (y)$ 在某区间**单调、可导**，且 $\varphi '(y) \ne 0$ ，则其反函数 $y=f(x)$ 在对应区间内也可导且 $\varphi '(y)=\frac 1 {f'(x)}$ 或 $\frac {dy}{dx}=\frac {1}{\frac {dx}{dy}}$

反函数的二阶导： $\varphi ''(y)=\frac {d \varphi '(y)}{d y}\frac {dy}{dx}$

### 参数方程求导法

设 $y=y(x)$ 是由参数方程 $\left\{ \begin{aligned} x=\varphi (t)  \\ y=\psi (t)\end{aligned} \right. (\alpha < t < \beta)$ 确定的函数，则

1. 若 $\varphi (t)$ 和 $\psi (t)$ 都可导，且 $\varphi '(t) \ne 0$ ，则 $\frac {dy}{dx}=\frac {\psi '(t)}{\varphi '(t)}$ .
2. 若 $\varphi (t)$ 和 $\psi (t)$ 二阶导，且 $\varphi '(t) \ne 0$ ，则 $\frac {d^2 y}{d x^2}=\frac {d}{dt}(\frac {\psi '(t)}{\varphi '(t)})\frac 1 {\varphi '(t)}$ .

>参数方程的二阶导是 $\frac {\frac {dy}{dx}}{dt} \frac {dt}{dx}=\frac 1 {\frac {dx}{dt}}$ ，不能直接把一阶导再对 $t$ 导

### 对数求导法

如果 $y=y(x)$ 的表达式由多个因式的**乘除、乘幂构成，或是幂指函数的形式**，可先将函数取对数，然后两边对 $x$ 求导。

### 高阶导数

$f^{(n)}(x_0)=\lim _{\triangle  x \rightarrow 0} \frac {f^{(n-1)}(x_0+\triangle  x)-f^{(n-1)}(x_0)}{\triangle x}=\lim _{  x \rightarrow x_0} \frac {f^{(n-1)}(x)-f^{(n-1)}(x_0)}{ x-x_0}$

**常用公式**：

1. $(\sin x)^{(n)}=\sin (x+n \cdot \frac \pi 2)$
2. $(\cos x)^{(n)}=\cos (x+n \cdot \frac \pi 2)$
3. $(u \pm v)^{(n)}=u^{(n)} \pm v^{(n)}$
4. $(uv)^{(n)}=\Sigma _{k=0} ^n C _n ^k u^{(k)} v^{(n-k)}$

常用方法：

1. 代公式
2. 求一阶 $y'$ ，二阶 $y''$ ，归纳 $n$ 阶导数 $y^{(n)}$
3. 利用泰勒公式（求特定点的导数值）[解析](https://www.bilibili.com/video/BV1J34y177ki/?spm_id_from=333.337.search-card.all.click&vd_source=d65b1656af047e1ce3454d2292c37d8b)

### 极坐标下的函数求导法

$x=rcos\theta$ ， $y=rsin\theta$ 转化为直角坐标，再使用[[#参数方程求导法]]解决
